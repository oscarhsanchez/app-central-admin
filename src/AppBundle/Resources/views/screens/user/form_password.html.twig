{% include 'ESocialUtilBundle:flash-messages:_flash.messages.html.twig' %}
{% include 'ESocialUtilBundle:flash-messages:_form.errors.html.twig' %}

{{ form_start(form, { method: 'POST', attr: { onsubmit: 'sendAjaxPopupForm(this); return false;' } , action: path('admin_user_password_update', {token: entity.token }) }) }}

{{ form_row(form.plainPassword) }}

{{ form_end(form) }}