{% include 'ESocialUtilBundle:flash-messages:_flash.messages.html.twig' %}
{% include 'ESocialUtilBundle:flash-messages:_form.errors.html.twig' %}

{{ form_start(form, { method: 'POST', attr: { onsubmit: 'sendAjaxPopupForm(this); return false;' } , action: entity is defined and entity.pkZona is not empty ? path('zone_update', {id: entity.token }) : path('zone_create', { type: type }) }) }}
{{ form_row(form.nombre) }}
{{ form_end(form) }}